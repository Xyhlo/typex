export const WELCOME_CONTENT = `# Welcome to TypeX

A beautifully crafted Markdown editor — built on **Tauri**, typed in **TypeScript**, rendered by **Milkdown**.

> The writing is the subject. Everything else gets out of the way.

## What's inside

- **Two hand-tuned themes** — _Obsidian Ink_ for night writing and _Ivory Paper_ for daylight sessions. Toggle with \`Ctrl+Shift+L\`.
- **Command palette** — \`Ctrl+K\` opens every action in the app.
- **Live outline** — headings stream into the sidebar as you type.
- **Focus mode**, **word counts**, **reading time**, and a **tab-per-document** workflow.

## Markdown that looks the way you'd want it to

### A real heading hierarchy

Heading weights, sizes, and spacing form a considered modular scale. Lowercase \`h1\` carries a soft accent underline; lowercase \`h6\` is a microcaps annotation.

### Inline formatting

You can write **bold**, _italic_, ~~strikethrough~~, \`inline code\`, and [links that animate on hover](https://typora.io). Highlights like ==this one== use a subtle accent wash.

### Code with taste

\`\`\`typescript
// Syntax highlighting via highlight.js, themed to match.
export const greet = (name: string): string => {
  return \`Hello, \${name}! Welcome to TypeX.\`;
};
\`\`\`

### Lists, nested cleanly

- Draft without friction.
- Tab and shift-tab just work.
  - Nested bullets keep their rhythm.
    - As deep as you need.

1. Ordered lists get their own marker color.
2. Numerals align with body weight.
3. Indentation matches the 4px grid.

### Task lists

- [x] Design tokens
- [x] Dark theme
- [x] Light theme
- [ ] Your first document

### Tables

| Shortcut | Action |
| --- | --- |
| \`Ctrl+N\` | New document |
| \`Ctrl+O\` | Open file |
| \`Ctrl+S\` | Save |
| \`Ctrl+K\` | Command palette |
| \`Ctrl+\\\` | Toggle sidebar |

### Quotes for emphasis

> Typography is the craft of endowing human language with a durable visual form.
>
> — _Robert Bringhurst_

---

Start typing to replace this. Your work is safe in memory — save when you're ready.
`;
