node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri
src-tauri/target
src-tauri/gen
# WiX fragment is machine-specific (absolute paths) — regenerated by
# `npm run wix-file-icons` (auto-run via beforeBuildCommand on every
# `tauri build`).
src-tauri/wix/file-icons.wxs
# Bundled sidecar binaries — fetched at build time, never committed
src-tauri/binaries

# Pandoc --extract-media artifact from manual imports
src-tauri/media

# Release notes staged for publishing (regenerate per version)
.release-notes-*.md

# Scratch files
color_test.py

# OS
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
